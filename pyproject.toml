[project]
name = "aotf"
version = "0.1.0.dev0"
description = "AOTF (Autonomous Optimization Task Flow) - Pre-MVP foundation only. Not for production use."
requires-python = ">=3.11,<3.14"
readme = "README.md"
dependencies = ["claude-agent-sdk>=0.2.152,<1"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
